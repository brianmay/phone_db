<h1>New Contact</h1>

<%= form_for @changeset, Routes.contact_path(@conn, :create), fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= text_input_field(f, :phone_number) %>
  <%= text_input_field(f, :name) %>
  <%= text_input_field(f, :comments) %>
  <%= select_field(f, :action, @actions) %>

  <div>
    <span><%= submit "Save", class: "btn btn-primary" %></span>
    <span><%= link "Back", to: Routes.list_contact_path(@conn, :index), class: "btn btn-secondary" %></span>
  </div>
<% end %>
